apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-card-component-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: profile-card-component-app
  template:
    metadata:
      labels:
        app: profile-card-component-app
    spec:
      containers:
        - name: profile-card-component-app
          image: profile-card-component-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
