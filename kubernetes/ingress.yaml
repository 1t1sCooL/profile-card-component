apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: profile-card-component
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /ProfileCardComponent
            pathType: Prefix
            backend:
              service:
                name: profile-card-component
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
